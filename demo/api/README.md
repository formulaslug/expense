# Finance Backend

The backend serves a REST API that connects to Mongoose.

## Getting Started

1. Install [Node JS](https://nodejs.org/en/download/) and [MongoDB](https://www.mongodb.com/download-center?jmp=nav)
2. Go to `~/api/`
3. `npm install`
4. Start MongoDB `mongod -dbpath db`
5. `node app.js`
